# 2026-05-24 工作日志

## 本次完成

### 1. 场景切换重构——推拉幕布过渡
- 删除水彩 shader（`watercolor_transition.gdshader` 保留文件）、噪声纹理、褪色覆盖层
- 新方案：纯色 ColorRect 幕布，左→滑入(0.3s) → 切场景+设 zoom → 右→滑出(0.3s)
- 颜色自动匹配各场景 `transition_color` 暗沉版（×0.35），不突兀
- 只改了 `scene_manager.gd`，接口不变，所有调用方正常
- 彻底消除之前的后处理分界线和水彩不平滑问题

### 2. 场景切换过渡设计讨论
- 对比了星露谷/Until Then（纯色淡入淡出）、塞尔达（圆形遮罩）、Celeste/Hollow Knight（推拉幕布）
- 选择推拉幕布方案：有方向感、零边界问题、实现简单、zoom 完全不可见
- 确认镜头 zoom 差异（室外 1.25 ↔ 室内 1.5↔1.8）是过渡不丝滑的核心原因
- 确认过渡系统完全独立，改它不影响其他系统

### 3. 建筑位置调整尝试（已回退）
- 尝试把建筑移到右边（x+800），出现后处理暗角分界线问题
- 经多次尝试修复（CanvasLayer、镜头跟随覆盖层、视差层扩展）均未能彻底解决
- 最终回退到 `247a20d` 版本，建筑留在原位
- 分界线的根因：ParallaxBackground 的 motion_scale 导致视差层在高镜头位移时偏移量超出场景内容范围

### 4. 碰撞修复
- 回退后发现玩家碰撞是全身 32×54（非脚下），建筑碰撞条挡住门前路径
- 修复：玩家碰撞改 20×10 脚下、建筑碰撞条缩进 8px 入建筑内部

### 5. Git 提交规范
- 统一为 Conventional Commits：`feat:/fix:/docs:/refactor:/style:`
- 写入 CLAUDE.md，后续按此规范提交

### 6. 交流风格调整
- 写入全局 CLAUDE.md：伙伴式沟通，颜文字 + 可爱语气尾调

## 修改文件

| 文件 | 操作 |
|------|------|
| `scripts/autoload/scene_manager.gd` | 重写（水彩→幕布） |
| `scripts/main.gd` | 修改（碰撞公式调整） |
| `scenes/player.tscn` | 修改（脚下碰撞） |
| `CLAUDE.md` | 修改（+提交规范+出生点规则） |
| `logs/2026-05-24.md` | 新建 |

## 犯的错误

### 建筑右移后的分界线
- 多次尝试修复（CanvasLayer 覆盖层、镜头跟随、视差层扩展）都失败
- 根因：ParallaxBackground motion_scale 让视差层偏移量超出场景内容范围，露出 Godot 紫色 clear color
- 教训：大范围调整建筑位置前，先计算 ParallaxBackground 各层在最大镜头位移下的覆盖范围

### 回退后碰撞不一致
- git checkout 从 247a20d 恢复了 scene_manager 和 main.tscn，但 player.tscn 碰撞还是全身的
- 原因：脚下碰撞的改动在 commit 之后做的（工作日志记录了但 commit 里没有）
- 教训：功能完成后立即 commit，不要攒着

### 13. 音效系统基础接入
- AudioManager autoload：BGM+4通道SFX池，play_bgm/play_sfx/stop_bgm
- 占位音效合成：ui_click(600Hz)、door_open(250→80Hz滑音)、footstep(噪声)
- AudioStreamWAV生成PCM数据，无缓冲依赖
- 暂停菜单设置面板：BGM/SFX音量滑块(-40~0dB)+实时dB显示
- 场景切换/标题按钮/暂停按钮/玩家脚步声全部接入

### 14. 场景构图系统
- 滚轮缩放 0.9~2.5，Tween 0.25s平滑，镜头偏移联动缩放
- 深度范围对齐地面线 360~520(160px)
- 天空-400~600、地面到900，适配最远视距
- 镜头偏移-100、limit_top=-500、drag_vertical=false
- 场景事物左右分布调整（建筑右移+其他左移）
- 清理无用色块（OldHouseEdge/ForegroundPost/小物件）
- 「场景构图方法论」写入策划案 06-场景与世界

### 15. 碰撞系统重构——深度碰撞
- 从219行手动碰撞代码重构为26行深度碰撞系统
- `_add_body(name, w, h, ground_y)` 一行注册
- `_physics_process` 按 |player.y - ground_y| < 25px 判定开关碰撞
- 只在地面线(y≈360)同深度的物体有碰撞

### 16. 遮挡关系确立
- z_index 视觉层 + CollisionBody 物理层，两层独立
- 前景物(y>390)不碰撞，仅视觉遮挡
- 树冠树干统一按树干落地点定深度(z_index=ground_y)
- 此规则写入策划案 + CLAUDE.md

### 17. 工具与流程
- 安装 Superpowers 技能套件 + grill-me 技能
- 确立 brainstorming/grill-me 使用策略：简单修bug跳过，新功能启用
- Git 提交规范统一为 Conventional Commits
- 全局 CLAUDE.md 加入交流风格（颜文字+可爱语气尾调）
- 每次 commit 后自动 git push

## 犯的错误

### 建筑移动后分界线
- 尝试把建筑移到右边时出现视口紫色分界线
- 多次尝试修复（CanvasLayer/镜头跟随/视差扩展）均失败
- 根因：ParallaxBackground motion_scale 导致视差层偏移超出内容范围
- 教训：大范围调整前先计算最大视距下的内容覆盖

### 碰撞手动管理太繁琐
- 每个物体单独写 `_add_static_body_at` 或 `_collide_at_bottom`
- 移动物体后要手动算坐标更新
- 改进：统一 `_add_body(name, w, h, ground_y)` + 深度判定自动开关

### AudioStreamGenerator 阻塞
- push_frame 在无消费者时阻塞导致游戏卡死
- 改用 AudioStreamWAV 直接写 PCM 数据

### 树冠树干 z_index 误差
- 树冠和树干共用父节点 z_index 导致深度排序不准
- 改进：多部分物体统一用最底部落地 Y 作为深度锚点

## 待继续
- 事件系统深化
- 更多场景搭建
- 美术素材制作
- NPC 时间/故事自由活动系统
- 美术素材制作
