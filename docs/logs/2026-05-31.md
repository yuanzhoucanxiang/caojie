# 2026-05-31 工作日志

## 本次完成

### 1. InteractableObject 行动事件接口

扩展 `scripts/triggers/interactable_object.gd`，让物品除普通查看外也能作为事件节点：

- 新增 `@export` 变量：`event_id`、`event_text`、`event_conditions`、`event_choices`、`completed_description`、`repeatable`
- 条件格式沿用 NPC：`event_completed:<id>`、`attr:<属性>>=<数值>`
- 非 repeatable 事件完成后显示 `completed_description` 或普通 `description`
- 行动触发点默认 `interaction_priority = 5`

### 2. 童年首周事件链 v1

替换五个 NPC 的事件数据，使用 `w1_` 前缀 ID，共 22 个事件：

| NPC | 主线事件 | 日常事件 |
|-----|---------|---------|
| 外婆 | arrival → feed_chickens_task → feed_chickens_return → homesick_evening | gm_daily_repeat |
| 舅妈 | kitchen_greeting → water_task → water_return | at_daily_repeat |
| 舅舅 | first_greeting → scooter_hint → softened | uc_daily_repeat |
| 二表哥 | invite_play → stone_table_task → old_house_dare | c2_daily_repeat |
| 小明 | first_meet → clothesline_task → friend_return | xm_daily_repeat |

每个 NPC 的 repeatable 日常事件放在事件列表最后，避免抢走主线事件。

### 3. 院落行动触发点

新增/更新 6 个 `InteractableObject` 行动触发点：

| 触发点 | 位置 | 事件 ID | 说明 |
|--------|------|---------|------|
| 养鸡场 | (185, 350) | `w1_act_feed_chickens` | 新建，大屋左下方 |
| 水井 | (576, 382) | `w1_act_fetch_water` | 更新，打水行动 |
| 石桌 | (392, 412) | `w1_act_stone_table_game` | 更新，小游戏 |
| 晾衣绳 | (1002, 342) | `w1_act_collect_clothes` | 更新，收衣服 |
| 摩托 | (220, 432) | `w1_act_check_scooter` | 更新，观察摩托 |
| 老屋门口 | (750, 370) | `w1_act_old_house_eaves` | 新建，老屋观察 |

### 4. 养鸡场视觉

在 `CourtyardStageBuilder` spec 中新增养鸡场元素：
- 鸡棚屋顶（140×16，深棕色）
- 鸡棚主体（124×28，木色）
- 竹栅栏（148×14）
- 饲料盆（22×8）
- 3 个小鸡椭圆占位

### 5. 测试

新增 `tests/unit/w1_event_chain_test.gd`，覆盖：
- w1_ 事件 ID 唯一性
- 外婆 arrival 无条件触发
- 完成 arrival 后触发 feed_chickens_task
- 非 repeatable 事件完成后跳过
- repeatable 日常不抢主线
- InteractableObject 条件门控

### 6. 行动触发点前置观察文本

新增 `pre_condition_description` 字段，条件未满足时显示生活化观察文本，不再是静默返回：

| 触发点 | 条件未满足时的文本 |
|--------|-------------------|
| 养鸡场 | 小鸡在栅栏里叽叽叫，看起来饿了。不过你还不知道该怎么喂它们。 |
| 水井 | 井口旁边放着一个旧水桶，井水在深处泛着微光。现在还不需要打水。 |
| 石桌 | 圆石桌被太阳晒得温热，桌面上有几道浅浅的划痕。 |
| 晾衣绳 | 衣服挂在绳上慢慢晃，有一件快要滑下来了。不过还没人让你帮忙收衣服。 |
| 摩托 | 一辆旧摩托停在院角，车身落了灰。舅舅说过不让乱碰。 |
| 老屋门口 | 老屋的门半掩着，看起来很久没人进去过了。一个人过去有点害怕。 |

### 7. NPC 比例尺占位系统

`npc_base.gd` 新增 `npc_body_size` 和 `prompt_offset_y`，用临时人形轮廓替代统一色块。

### 8. 文档同步

- `architecture_handoff.md`：补充行动事件接口文档和首周事件链规则
- `courtyard_stage_handoff.md`：补充院落行动点分布表
- `npc_event_selection_test.gd`：适配 w1_ 事件链

## 提交记录

| 提交 | 内容 |
|------|------|
| `396fd52` | feat: 童年首周事件链 v1——NPC事件+行动触发点+养鸡场 |
| `503a84c` | docs: 5/31工作日志 |
| `1d6c7a0` | fix: 更新旧事件测试适配 w1_ 事件链 |
| `a2a423b` | feat: NPC比例尺占位系统+首周事件文档补充 |
| `fe3355d` | feat: 行动触发点条件未满足时显示前置观察文本 |

## 待继续

- 人工验收首周事件链完整闭环
- 院落远景/中景素材逐步替换
- 天台梯形透视场景
- NPC 时间/故事自由活动系统
